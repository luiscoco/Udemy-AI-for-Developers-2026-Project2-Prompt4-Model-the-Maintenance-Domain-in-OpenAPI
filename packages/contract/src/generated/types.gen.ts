// This file is auto-generated by @hey-api/openapi-ts

export type ClientOptions = {
    baseUrl: `${string}://${string}` | (string & {});
};

export type HealthStatus = {
    status: 'ok';
};

/**
 * Lifecycle state of a work order. `completed` and `cancelled` are final.
 */
export type WorkOrderState = 'reported' | 'triaged' | 'scheduled' | 'in_progress' | 'completed' | 'cancelled';

/**
 * Lifecycle transition. triage: reported→triaged; schedule: triaged→scheduled; start: scheduled→in_progress; complete: in_progress→completed; cancel: triaged|scheduled→cancelled.
 */
export type WorkOrderAction = 'triage' | 'schedule' | 'start' | 'complete' | 'cancel';

export type Priority = 'low' | 'medium' | 'high' | 'critical';

export type Asset = {
    id: string;
    /**
     * Human-readable asset tag (e.g. a plate or inventory code).
     */
    tag: string;
    name: string;
    location: string;
};

export type Technician = {
    id: string;
    name: string;
    specialty: string;
};

export type WorkOrder = {
    id: string;
    /**
     * Human-readable work order reference, assigned by the server.
     */
    reference: string;
    assetId: string;
    title: string;
    description: string;
    priority: Priority;
    state: WorkOrderState;
    /**
     * Assigned technician, or null while unassigned. Always present.
     */
    technicianId: string | null;
    reportedAt: string;
    updatedAt: string;
};

export type NewWorkOrder = {
    assetId: string;
    title: string;
    description: string;
    priority: Priority;
};

export type TransitionCommand = {
    action: WorkOrderAction;
};

export type AssignmentCommand = {
    technicianId: string;
};

export type DashboardSummary = {
    /**
     * Work orders not in a final state (completed or cancelled).
     */
    totalOpen: number;
    /**
     * Open work orders with priority `critical`.
     */
    criticalOpen: number;
    /**
     * Open work orders with no technician assigned.
     */
    unassigned: number;
    /**
     * Count of work orders per state. Every state is always present.
     */
    byState: {
        reported: number;
        triaged: number;
        scheduled: number;
        in_progress: number;
        completed: number;
        cancelled: number;
    };
};

export type ApiError = {
    message: string;
    /**
     * Optional structured context about the error (e.g. field errors).
     */
    details?: {
        [key: string]: unknown;
    };
};

export type GetHealthData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/health';
};

export type GetHealthResponses = {
    /**
     * Service is healthy
     */
    200: HealthStatus;
};

export type GetHealthResponse = GetHealthResponses[keyof GetHealthResponses];
